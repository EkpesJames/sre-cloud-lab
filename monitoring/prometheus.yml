global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: "cloud-lab"
    static_configs:
      - targets: ["cloud-app:80"]

alerting:
  alertmanagers:
    - static_configs:
        - targets: ["alertmanager:9093"]
